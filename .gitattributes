# Normalize line endings: store as LF in the repo, check out native on each OS.
* text=auto

# Explicit for the files we track
*.py   text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.diff text eol=lf
